/**
 * types_compat.h - Legacy compatibility wrapper for type aliases
 *
 * Prefer including types.h directly. This header exists so older sources
 * that still reference types_compat.h keep building without redefining the
 * shared aliases twice.
 */

#ifndef TYPES_COMPAT_H
#define TYPES_COMPAT_H

#include "types.h"

#endif /* TYPES_COMPAT_H */
